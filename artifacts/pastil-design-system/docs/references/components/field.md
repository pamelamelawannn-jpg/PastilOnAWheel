# Field

- **Source:** `artifacts/pastil-tracker/src/App.tsx`, `FieldLabel` line 199; `artifacts/pastil-tracker/src/index.css`, `.field` lines 111–122.
- **Visual contract:** muted label above a full-width input; 0.8rem radius; border uses input token; warm page background; 0.78rem/0.9rem padding; primary border and soft ring on focus.
- **Composition:** keep labels short; use a hint for local-only or future-sync context instead of adding a long explanation.